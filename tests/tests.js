var expect = require("chai").expect;
var uploader = require("../upload-to-youtube");

describe("Example", function() {
  describe("Doing something", function() {
    it("should be true", function() {      
      expect(true).to.equal(true);
    });
  });
});